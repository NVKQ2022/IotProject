@* @{
    ViewData["Title"] = "Home Page";
@model User

}

@{
    User me = new User();
    me.Name = "quan";
}

<div class="text-center">
    <h1 class="display-4">Welcome @me.Name</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
    <p>hello world i am nguyen viet ky quan. im from an giang the wonderland.Today i am going to show you my  typing skill. What the fuck?! ghe  </p>
</div>

 *@



@* 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Live Camera Viewer</title>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-color: #1e1e2f;
            color: #f0f0f0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            display: flex;
            flex-direction: column;
            /* align-items: center; */
            min-height: 100vh;
            padding: 2rem;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            color: #00bcd4;
            text-shadow: 1px 1px 5px rgba(0, 188, 212, 0.3);
        }

        .camera-container {
            background: #b0b0b0 ;
            padding: 1.5rem;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            max-width: 1000px;
            width: 95%;
        }

        img {
            width: 100%;
            height: auto;
            border-radius: 10px;
            border: 2px solid #00bcd4;
        }

        .status {
            margin-top: 0.5rem;
            font-size: 0.9rem;
            color: #aaa;
        }
    </style>
</head>
<body>
    <h1>🌲 Forest Monitoring - Live Camera Feed</h1>
    <div class="camera-container">
        <img src="/Home/Proxy" alt="Live stream is currently offline or unavailable." />
        <div class="status">Status: Live feed from IP Webcam</div>
    </div>
</body>
</html>
 *@
@* 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Live Camera Viewer</title>
    <link rel="stylesheet" href="~/css/Camera.css" />
    <style>
    
         * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

         body {
            background-color: /* #1e1e2f; */ #f0f0f0;
            color: #f0f0f0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
           /*  padding: 2rem; */
        } 

        h1 {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            color: #00bcd4;
            text-shadow: 1px 1px 5px rgba(0, 188, 212, 0.3);
        }

        .camera-wrapper {
            display: flex;
            justify-content: center;
        }

        .camera-container {
            background: #2a2a3d;
            padding: 1.5rem;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
            max-width: 1000px;
            width: 95%;
            position: relative;
        }

        img {
            width: 100%;
            height: auto;
            border-radius: 10px;
            border: 2px solid #00bcd4;
            display: block;
        }

        .status {
            margin-top: 0.5rem;
            font-size: 0.9rem;
            color: #aaa;
            text-align: center;
        }

        /* Spinner styles */
        .spinner {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            border: 6px solid #f3f3f3;
            border-top: 6px solid #00bcd4;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            animation: spin 1s linear infinite;
            z-index: 10;
        }

        .camera-box {
            width: 750px;
            height: 400px;
            border: 1.5px solid #00bcd4;
            border-radius: 10px;
            overflow: hidden;
            margin: 0 auto;
            background: #111;
            position: relative;
        }

            .camera-box img {
                width: 100%;
                height: 100%;
                object-fit: cover; /* Alternatives: contain / fill / none */
                display: block;
            }
        


        .hidden {
            display: none;
        }
    </style>
</head>
<body>
    <h1>🌲 Forest Monitoring - Live Camera Feed (udp reconstruct)</h1>
    <div class="camera-wrapper">
        <div class="camera-container"> *@
            @* <div class="spinner" id="spinner"></div>
            <div class="camera-box">
                <img id="cameraFeed" src="/Home/Proxy" alt="Live stream is offline"
                     onload="hideSpinner()" onerror="hideSpinner()" />
            </div> *@
            @* <img id="cameraFeed" src="/Home/Proxy1" alt="Live stream is offline" onload="hideSpinner()" onerror="hideSpinner()" /> *@
           
            
            
            @* <h1>Live Stream</h1> <img src="/stream/cam01" width="640" />
            <h1>Live Stream SSE</h1> <img src="/sse/cam01" width="640" /> *@


@* 
            <div class="status">Status: Live feed from IP Webcam (auto-refresh every 10s)</div>
        </div>
    </div>

    <script>
        const cameraFeed = document.getElementById("cameraFeed");
        const spinner = document.getElementById("spinner");

        function hideSpinner() {
            spinner.classList.add("hidden");
        }

        function refreshFeed() {
            spinner.classList.remove("hidden");
            const timestamp = new Date().getTime();
            cameraFeed.src = "/Home/Proxy?t=" + timestamp;
        }

        // Refresh every 10 seconds
        setInterval(refreshFeed, 10000);
    </script>
</body>
</html>
 *@

<div class="max-w-3xl mx-auto bg-white shadow-xl rounded-2xl p-8 mt-10 border border-gray-200">
    <h2 class="text-2xl font-bold text-gray-800 mb-4">Báo Cáo Đề Tài</h2>
    <p class="text-lg text-gray-600 mb-2"><strong>Đề tài:</strong> Hệ thống camera an ninh sử dụng Raspberry Pi</p>
    <p class="text-lg text-gray-600 mb-2"><strong>Môn học:</strong> Hệ thống nhúng và Mạng không dây</p>
    <p class="text-lg text-gray-600 mb-2"><strong>Mã môn học:</strong> NT131.P22</p>
    <p class="text-lg text-gray-600 mb-6"><strong>Giáo viên hướng dẫn:</strong> Thầy Lê Anh Tuấn</p>

    <h3 class="text-xl font-semibold text-gray-700 mb-3">Thành viên nhóm 15:</h3>
    <ul class="list-disc list-inside text-gray-700 space-y-1">
        <li><strong>Nguyễn Việt Kỳ Quân</strong> – MSSV: 23521267</li>
        <li><strong>Cao Minh Quang</strong> – MSSV: 23521277</li>
    </ul>
</div>
<link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">



<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Hệ thống giám sát cháy rừng</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">

    <style>
        .navbar-brand {
            font-weight: bold;
        }

        .alert-fire {
            background-color: red;
            color: white;
            font-weight: bold;
            text-align: center;
            display: none;
        }

        footer {
            text-align: center;
            padding: 10px;
            background-color: #f8f9fa;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>
@* <body> *@

    <!-- 🔥 Thanh cảnh báo cháy -->
    <div id="fireAlert" class="alert alert-fire">
        🚨 Cảnh báo! Cháy rừng phát hiện tại <span id="fireLocation"></span> 🚨
    </div>
    
    <!-- 🌲 Navbar điều hướng -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">Security Camera</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item"><a class="nav-link" href="/Home/Index"> Introduction</a></li>
                    @* <li class="nav-item"><a class="nav-link" href="/Camera/CameraView">Camera</a></li> *@
                
                <li class="nav-item"><a class="nav-link" href="/DeviceControl/Index">Control</a></li>

                </ul>
                
                <!-- 👤 Hiển thị tài khoản đăng nhập -->
                
            </div>
        </div>
        @* <partial name="_LoginPartial" /> *@
        
        @* <div class="d-flex"> *@
        <a class="btn btn-custom btn-sm me-2 px-3 py-1 fw-semibold" href="/Authenticate/MyLogin">
                Login
            </a>
        <a class="btn btn-custom btn-sm me-2 px-3 py-1 fw-semibold" href="/Authenticate/MyRegister">
                Register

            </a>
        @* </div> *@

    </nav>

    <!-- 🚀 Nội dung chính -->
<div class="container">
    <main role="main" class="pb-3">
        @RenderBody()
    </main>
</div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    @RenderSection("Scripts", required: false)

@* </body> *@
</html>

<style>
    .btn-custom {
        background-color: white;
        color: black;
        border: none;
        transition: background-color 0.3s, color 0.3s;
    }

        .btn-custom:hover {
            background-color: #e0e0e0; /* Slightly darker white */
            color: black;
        }
</style>